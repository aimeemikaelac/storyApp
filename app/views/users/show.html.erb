<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>

<%= image_tag @user.photo.url(:small) %> <br />

<% if @user.id == session[:user_id] %>
	<%= edit_path(@user) %>
	<%= delete_path(@user) %> 
<% end %>
