<%= form_for(@shared_story) do |f| %>
  <% if @shared_story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shared_story.errors.count, "error") %> prohibited this shared_story from being saved:</h2>

      <ul>
      <% @shared_story.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :story_id %><br />
    <%= f.number_field :story_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
